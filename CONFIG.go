package main 

import (
	"time"
)

const(
	SERVER_PORT = ":8080"
	STICKERS_PATH = "/stickers"
	BOOKS_PATH = "/books"
	LOGIN_PATH = "/login"
	POSTS_PATH = "/posts"
	USERS_PATH = "/users"
	REQUEST_TIMEOUT = time.Second*10
)